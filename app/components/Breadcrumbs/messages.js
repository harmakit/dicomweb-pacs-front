import { defineMessages } from 'react-intl';

export const scope = 'app.components.Breadcrumbs';

export default defineMessages({
  studies: {
    id: `${scope}.studies`,
  },
  study: {
    id: `${scope}.study`,
  },
  series: {
    id: `${scope}.series`,
  },
  instance: {
    id: `${scope}.instance`,
  },
});
