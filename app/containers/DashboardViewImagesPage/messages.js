import { defineMessages } from 'react-intl';

export const scope = 'app.containers.DashboardViewImagesPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'View images!',
  },
});
